<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/sort_by_world"
            android:icon="@android:drawable/ic_menu_sort_by_size"
            android:title="按INDEX排序" />
        <item
            android:id="@+id/sort_by_default"
            android:enabled="true"
            android:icon="@android:drawable/ic_menu_sort_alphabetically"
            android:title="按批注排序" />
        <item
            android:id="@+id/sort_by_index_first"
            android:icon="@android:drawable/ic_menu_search"
            android:title="按INDEX首位过滤" />
        <item
            android:id="@+id/search_index"
            android:icon="@android:drawable/ic_search_category_default"
            android:title="INDEX精确搜索" />
        <item
            android:id="@+id/sort_by_note_first"
            android:icon="@android:drawable/ic_menu_search"
            android:title="按批注首位过滤" />
        <item
            android:id="@+id/search_note"
            android:icon="@android:drawable/ic_search_category_default"
            android:title="批注精确搜索" />
    </group>

    <item android:title="Other">
        <menu>
            <item
                android:id="@+id/online_search"
                android:icon="@android:drawable/ic_menu_mapmode"
                android:title="在线查询" />
            <item
                android:id="@+id/offline_document"
                android:icon="@android:drawable/ic_menu_edit"
                android:title="离线手册" />
        </menu>
    </item>

</menu>
